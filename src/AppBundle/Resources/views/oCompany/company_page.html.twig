{% extends 'AppBundle::base.html.twig' %}


{% block body %}

    {% for oUser in oUsers %}
    <h2>Welcome {{ oUser.username }}!</h2>
        {% endfor %}
    <h4>Здесь вы можете добавить продукт!</h4>
        <ul>
            <li>
                <a href="{{ path('new_new') }}">Добавить новый продукт</a>
            </li>
            <li>
                <a href="{{ path('logout') }}">Выйти</a>
            </li>
        </ul>
    <div id="bc1" class="btn-group btn-breadcrumb">
        <a class="btn btn-lg btn-primary btn-block" href="{{path('my_product')}}"><i class="fa fa-home"></i>Мои продукты</a>
    </div>

{% endblock %}